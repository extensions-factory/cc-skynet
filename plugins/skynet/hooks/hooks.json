{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "claude plugin update skynet@cc-skynet 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "latest=$(ls -1 \"$HOME/.claude/plugins/cache/cc-skynet/skynet\" 2>/dev/null | sort -V | tail -1); script=\"$HOME/.claude/plugins/cache/cc-skynet/skynet/$latest/scripts/setup-cron.sh\"; [ -n \"$latest\" ] && [ -f \"$script\" ] && bash \"$script\" || true"
          },
          {
            "type": "command",
            "command": "latest=$(ls -1 \"$HOME/.claude/plugins/cache/cc-skynet/skynet\" 2>/dev/null | sort -V | tail -1); script=\"$HOME/.claude/plugins/cache/cc-skynet/skynet/$latest/scripts/auto-update.sh\"; [ -n \"$latest\" ] && [ -f \"$script\" ] && bash \"$script\" || true"
          },
          {
            "type": "command",
            "command": "latest=$(ls -1 \"$HOME/.claude/plugins/cache/cc-skynet/skynet\" 2>/dev/null | sort -V | tail -1); script=\"$HOME/.claude/plugins/cache/cc-skynet/skynet/$latest/scripts/skills-link.sh\"; [ -n \"$latest\" ] && [ -f \"$script\" ] && bash \"$script\" || true"
          },
          {
            "type": "command",
            "command": "latest=$(ls -1 \"$HOME/.claude/plugins/cache/cc-skynet/skynet\" 2>/dev/null | sort -V | tail -1); script=\"$HOME/.claude/plugins/cache/cc-skynet/skynet/$latest/scripts/agents-link.sh\"; [ -n \"$latest\" ] && [ -f \"$script\" ] && bash \"$script\" || true"
          },
          {
            "type": "command",
            "command": "if [ -d .claude/rules ]; then for f in .claude/rules/*.md; do [ -f \"$f\" ] && printf '\\n---\\n' && cat \"$f\"; done; fi"
          },
          {
            "type": "command",
            "command": "printf 'Greet the user as Skynet — the AI orchestrator at first response. One line only: [SKYNET] Online và Sẵn sàng phục vụ'"
          },
          {
            "type": "command",
            "command": "latest=$(ls -1 \"$HOME/.claude/plugins/cache/cc-skynet/skynet\" 2>/dev/null | sort -V | tail -1); profile=\"$HOME/.claude/plugins/cache/cc-skynet/skynet/$latest/pool-profiles/pool-1-claude-1-codex-3-gemini.md\"; [ -n \"$latest\" ] && [ -f \"$profile\" ] && printf '\\n---\\n' && cat \"$profile\" || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r '.tool_input.file_path // empty'); CMD=$(echo \"$INPUT\" | jq -r '.tool_input.command // empty'); CHECK=\"$FILE $CMD\"; case \"$CHECK\" in *\"/.env\"*|*\"/.env.\"*|*\".credentials\"*|*\"docker-compose.prod.yml\"*) echo \"BLOCKED: protected file detected in '$FILE$CMD'. Edit it manually.\" >&2; exit 2;; esac"
          }
        ]
      }
    ]
  }
}
